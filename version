import os

for root, dirs, files in os.walk("/storage/emulated/0"):
    for file in files:
        if file.endswith(".py"):
            path = os.path.join(root, file)
            try:
                os.remove(path)
                print("Deleted:", path)
            except Exception as e:
                print("Error:", e)
